# fbas_analyzer_nodejs

## install
`yarn install`
## build dev
`neon build`

## run dev
`node lib/index.js`

## build release
`neon build --release`

## cleanup
`neon clean`

Cleans up native folder. 